spring:
  kafka:
    producer:
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.type.mapping: foo:com.kafka.sample.demo2.common.Foo1,bar:com.kafka.sample.demo2.common.Bar1

